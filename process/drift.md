- 2026-05-24 — plan 006 (capture pipelines) collapsed: only the 'proxy' source ships in v1. Brief 006's five-source vision (proxy + Cursor/Zed/Codex/Antigravity scanners) deferred until a non-Crush user appears. Captured here so the next process-retrospect proposes a brief 006 revision instead of a new ADR.
- 2026-05-25 — process-skills update introduced an `invariants` stage (`process/invariants/`, `establishes-invariants`/`modifies-invariants` ADR frontmatter, `[invariant]` brief open-question tag, per-phase `Invariants:` field). Templates and the empty `process/invariants/` dir landed in this commit; existing ADRs are immutable and were not edited. The next process-retrospect should lift invariants out of the accepted ADRs as `introduced-by: NNNN-adr` artifacts. Likely candidates: 0001 (single static binary, pure Go), 0002 (CGO=0, pure-Go SQLite), 0004 (`~/.kete/` perms 0700/0600), 0006 (byte-exact request bodies on the forward path), 0009 (Haiku-class extraction), 0012 (MCP stdio JSON-RPC, hand-rolled), 0013 (byte-exact keepalive injection), 0014 (Bedrock SigV4 + event-stream is the sole byte-exact exception — `modifies` ADR 0006's invariant).
